// ======================================================================
// Phase overlay: Skills test
// ======================================================================

/** @type {import('@walkthrough-loaders').WalkthroughOverlay} */
export default {
  id: 'phase:skills-test',
  rules: [
    // Example: append parallel parking instructions
    // { op: 'appendSteps', match: { section: 'Backing' }, steps: [ { script: 'Parallel park' } ] },
  ],
}