// ======================================================================
// Phase overlay: Pre-trip inspection
// ======================================================================

/** @type {import('@walkthrough-loaders').WalkthroughOverlay} */
export default {
  id: 'phase:pre-trip',
  rules: [
    // Example: keep only pre-trip related sections
    // { op: 'replaceSectionSteps', match: { section: 'Post-trip' }, steps: [] },
  ],
}